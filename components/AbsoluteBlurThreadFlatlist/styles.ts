import { StyleSheet, Dimensions } from "react-native";
import Colors from "../../constants/Colors";

export const styles = StyleSheet.create({
  threadMessageFlatlist: {
    paddingHorizontal: 5,
    marginBottom: 5,
  },
});

export default styles;
