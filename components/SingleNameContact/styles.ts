import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    justifyContent: "flex-start",
  },

  contactNameText: {
    fontSize: 12,
    textAlign: "center",
    color: "white",
    fontWeight: "600",
    paddingHorizontal: 2,
  },
});
