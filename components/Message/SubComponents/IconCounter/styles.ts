import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
  },
  likeText: {
    color: "white",
    marginHorizontal: 5,
    fontSize: 23,
    fontWeight: "500",
  },
});
